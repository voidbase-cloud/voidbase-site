import { defineHead } from "void";
export const head = defineHead(() => ({
  title: "Track it in a directory the binary serves - Docs - voidbase",
  description: "Take a running instance's collections and hooks into a git repository, stay on your own machine, and keep serving it with the standalone executable.",
}));

import { defineHead } from "void";
export const head = defineHead(() => ({
  title: "Track it as a voidbase stack - Docs - voidbase",
  description: "Fold a running instance into a Void application so the site and the backend are one repository and one Worker.",
}));

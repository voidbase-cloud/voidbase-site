import { defineHead } from "void";
export const head = defineHead(() => ({
  title: "Track it as a voidbase project - Docs - voidbase",
  description: "Take a running instance's collections and hooks into a git repository with npm, which is the layout that deploys to your own Cloudflare account.",
}));

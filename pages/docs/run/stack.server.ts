import { defineHead } from "void";

export const head = defineHead(() => ({
  title: "Start a stack app - Docs - voidbase",
  description: "A Void application with a voidbase backend inside, from an empty directory to one Worker.",
}));

import { defineHead } from "void";

export const head = defineHead(() => ({
  title: "pb_data - Docs - voidbase",
  description: "The database, the uploaded files and the generated typings a running instance owns.",
}));

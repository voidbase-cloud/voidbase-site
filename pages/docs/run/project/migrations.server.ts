import { defineHead } from "void";

export const head = defineHead(() => ({
  title: "pb_migrations - Docs - voidbase",
  description: "The schema as code: applied once, in order, carried with the deploy.",
}));

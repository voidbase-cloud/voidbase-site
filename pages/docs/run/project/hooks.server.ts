import { defineHead } from "void";

export const head = defineHead(() => ({
  title: "pb_hooks - Docs - voidbase",
  description: "Endpoints, event handlers and scheduled work, written in JavaScript and run inside the instance.",
}));

import { defineHead } from "void";

export const head = defineHead(() => ({
  title: "pb_secrets - Docs - voidbase",
  description: "One declaration for a project's configuration, with every key saying who may read it.",
}));

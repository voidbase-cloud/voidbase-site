import { defineHead } from "void";

export const head = defineHead(() => ({
  title: "Standalone executable - Docs - voidbase",
  description: "Download one file, unzip it, run it. No npm, no build step.",
}));

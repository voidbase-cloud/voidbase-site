import { defineHead } from "void";

export const head = defineHead(() => ({
  title: "voidbase cloud - Docs - voidbase",
  description: "Instances from a page, in your own Cloudflare account, with a repository if you want one, and a CLI for every button.",
}));

import { defineHead } from "void";

export const head = defineHead(() => ({
  title: "Create a project - Docs - voidbase",
  description: "Scaffold a voidbase project or start from a template, extend it with hooks and migrations, type the client from it, and deploy it to Cloudflare.",
}));

import { defineHead } from "void";

export const head = defineHead(() => ({
  title: "vb_secrets - Docs - voidbase",
  description: "One declaration for a stack app's configuration, across the server, the build and the browser.",
}));

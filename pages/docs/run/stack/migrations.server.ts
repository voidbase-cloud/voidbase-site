import { defineHead } from "void";

export const head = defineHead(() => ({
  title: "vb_migrations - Docs - voidbase",
  description: "Collections for a stack app, and how they differ from the app's own Drizzle tables.",
}));

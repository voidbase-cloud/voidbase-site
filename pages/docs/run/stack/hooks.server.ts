import { defineHead } from "void";

export const head = defineHead(() => ({
  title: "vb_hooks - Docs - voidbase",
  description: "Event handlers in TypeScript that run whenever a record changes, whoever changed it.",
}));

import { defineHead } from "void";

export const head = defineHead(() => ({
  title: "Instances from the CLI - Docs - voidbase",
  description: "Create, list and delete voidbase instances by name, on this machine, on a tunnel, on Cloudflare's local runtime or in your own account, and move data between them.",
}));

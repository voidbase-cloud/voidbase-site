import { defineHead } from "void";

export const head = defineHead(() => ({
  title: "Instances from the CLI - Docs - voidbase",
  description: "Create, list and delete voidbase instances by name, on this machine or on your own Cloudflare account.",
}));

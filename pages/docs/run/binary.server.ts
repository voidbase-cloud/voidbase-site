import { defineHead } from "void";
export const head = defineHead(() => ({
  title: "A directory the binary serves - Docs - voidbase",
  description: "Tracked hooks and schema, served by the standalone executable. No npm, no build, no Cloudflare.",
}));

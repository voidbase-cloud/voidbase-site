import { defineHead } from "void";
export const head = defineHead(() => ({
  title: "Publish a template - Docs - voidbase",
  description: "What makes a voidbase template worth listing, the seven checks the audit runs, and how to submit one.",
}));

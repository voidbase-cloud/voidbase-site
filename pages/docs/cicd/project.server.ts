import { defineHead } from "void";
export const head = defineHead(() => ({
  title: "A pipeline for a voidbase project - Docs - voidbase",
  description: "Workers Builds on every push, preview instances per branch, and the configuration the build is allowed to read.",
}));

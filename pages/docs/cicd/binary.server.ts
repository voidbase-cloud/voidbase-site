import { defineHead } from "void";
export const head = defineHead(() => ({
  title: "A pipeline for a directory the binary serves - Docs - voidbase",
  description: "No Cloudflare in the loop: check the directory on every push, then move it to your server and restart the service.",
}));

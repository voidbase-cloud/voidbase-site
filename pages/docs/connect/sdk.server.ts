import { defineHead } from "void";

export const head = defineHead(() => ({
  title: "Connect with the SDK - Docs - voidbase",
  description: "Read and write records, sign users in and subscribe to changes with the PocketBase SDK.",
}));

import { defineHead } from "void";

export const head = defineHead(() => ({
  title: "Connect with the admin panel - Docs - voidbase",
  description: "The unmodified PocketBase admin panel, served at /_/ by every instance, with the API reference and the MCP server beside it.",
}));

import { defineHead } from "void";
export const head = defineHead(() => ({
  title: "Pricing - Docs - voidbase",
  description: "voidbase is free. It runs in your own Cloudflare account, so the only bill is theirs, and here is how a free backend pays for itself.",
}));

import { defineHead } from "void";
export const head = defineHead(() => ({
  title: "What is an instance - Docs - voidbase",
  description: "One running voidbase: a database, its files, the collections you defined and an address. What it carries, the three ways to run it, and what decides between them.",
}));

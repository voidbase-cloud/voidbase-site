import { defineHead } from "void";
export const head = defineHead(() => ({
  title: "What is an instance - Docs - voidbase",
  description: "One running voidbase: a database, its files, the collections you defined and an address. The shapes it comes in, and what decides between them.",
}));

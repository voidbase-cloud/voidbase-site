import { defineHead } from "void";
export const head = defineHead(() => ({
  title: "Roadmap - Docs - voidbase",
  description: "The gaps other backends showed us, and the plugins, backups and previews we are building anyway.",
}));

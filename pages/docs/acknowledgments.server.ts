import { defineHead } from "void";

export const head = defineHead(() => ({
  title: "Acknowledgments - Docs - voidbase",
  description: "PocketBase, Void, Cloudflare and the open source projects voidbase is built out of.",
}));

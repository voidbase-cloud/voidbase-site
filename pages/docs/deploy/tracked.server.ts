import { defineHead } from "void";

export const head = defineHead(() => ({
  title: "What git tracks - Docs - voidbase",
  description: "Everything except the values of your secrets, and how those reach production instead.",
}));

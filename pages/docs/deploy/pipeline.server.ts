import { defineHead } from "void";

export const head = defineHead(() => ({
  title: "Deploy on every push - Docs - voidbase",
  description: "Connect a repository to Cloudflare Workers Builds so every push deploys the instance.",
}));

import { defineHead } from "void";
export const head = defineHead(() => ({
  title: "Compared to Firebase - Docs - voidbase",
  description: "Whose account the database lives in, and what each one is better at.",
}));

import { defineHead } from "void";
export const head = defineHead(() => ({
  title: "What we are going to fix - Docs - voidbase",
  description: "The gaps the comparison pages turned up, and the design for closing each one.",
}));

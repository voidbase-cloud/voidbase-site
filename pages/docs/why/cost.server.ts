import { defineHead } from "void";
export const head = defineHead(() => ({
  title: "What it costs - Docs - voidbase",
  description: "A cost model against Supabase, from published list prices, with every assumption shown.",
}));

import { defineHead } from "void";
export const head = defineHead(() => ({
  title: "Compared to Supabase - Docs - voidbase",
  description: "Postgres and a large ecosystem, against a self-host that is one command.",
}));

import { defineHead } from "void";
export const head = defineHead(() => ({
  title: "Compared to Encore - Docs - voidbase",
  description: "A framework for building a backend, against a backend that already runs.",
}));

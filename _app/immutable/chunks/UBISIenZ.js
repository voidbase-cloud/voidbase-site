import{I as e,P as t,X as n,_ as r,bt as i,gt as a,ht as o,it as s,nt as c,o as l,tt as u,yt as d}from"./BDLcFjNi.js";import"./xihTtKlq.js";import"./C3kXMaAy.js";var f=e(`<p>To execute multiple queries in a transaction you can use <a target="_blank" rel="noopener noreferrer"><code>app.RunInTransaction(fn)</code></a> .</p> <p>The DB operations are persisted only if the transaction returns <code>nil</code>.</p> <p>It is safe to nest <code>RunInTransaction</code> calls as long as you use the callback's <code>txApp</code> argument.</p> <div class="alert alert-info m-b-xs"><div class="icon"><i class="ri-information-line"></i></div> <div class="content"><p>Inside the transaction function always use its <code>txApp</code> argument and not the original <code>app</code> instance because we allow only a single writer/transaction at a time and it could
            result in a deadlock.</p> <p>To avoid performance issues, try to minimize slow/long running tasks such as sending emails,
            connecting to external services, etc. as part of the transaction.</p></div></div>`,1);function p(e,p){a(p,!1),l();var m=f(),h=c(m),g=s(u(h));d(),i(h),d(6),n(()=>r(g,`href`,`https://pkg.go.dev/github.com/pocketbase/pocketbase/core#BaseApp.RunInTransaction`)),t(e,m),o()}export{p as t};